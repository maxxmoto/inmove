meta:
  environment: python
  name: inmove
  default-command: python bot_runner.py
build:
  requirements:
    - flask
    - pyTelegramBotAPI
    - Pillow
    - requests
run:
  command: python bot_runner.py
  container-port: 5000
  working-directory: /app
